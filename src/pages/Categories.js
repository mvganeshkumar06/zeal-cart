import React from "react";
import "../css/DriftUI.css";
import styles from "../css/Categories.module.css";

const Categories = () => {
	return (
		<div className={`align-items-col ${styles.container}`}>
			<h1>Categories</h1>
		</div>
	);
};

export default Categories;
